#!/usr/bin/node

// Searches for the second largest integer
// in a list of integers passed as arguments

const arguments = parseInt(process.argv.length - 2);

for (let i = 0; i < arguments; i++) {
	console.log(arguments[i]);
}

//console.log(integers);
